## Fake Data Generator (CSV + SQLite)
### 📌 Overview

This project generates realistic synthetic (dummy) data using Python, Pandas, and Faker.
The generated data can be used for testing, development

The project demonstrates how to:

Generate fake datasets programmatically

Save data to CSV files

Load data into a SQLite database

Read data back for validation

### 🚀 Features

Generate realistic dummy Employee data

Export generated data to CSV format

Store data in a SQLite database table

Simple and reusable for testing data pipelines

### 🛠️ Technologies Used

Python

Pandas

Faker

SQLite3
